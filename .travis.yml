language: python

python:
  - 2.7

before_install:
  - pip install codecov nose2 nose2-cov

install:
  - pip install -r requirements.txt
  - pip install -r test-requirements.txt

script:
  - nose2 -v -C --cover-package=irobot --cover-inclusive

after_success:
  - codecov
